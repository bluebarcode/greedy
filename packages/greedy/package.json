{
  "name": "@aic-greedy/core",
  "extends": "../../tsconfig.json",
  "version": "0.0.0",
  "compilerOptions": {
    "outDir": "./dist"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "include": [
    "./src"
  ],
  "scripts": {
    "build": "tsc",
    "test": "jest --config=jest.config.js",
    "prepublish": "npm test"
  }
}
